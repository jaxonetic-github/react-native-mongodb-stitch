//import {  } from '../../../../redux/types';

/**       
 * Reducer for the videoReferences part of state
 */
const videoRefsReducer = (state={}, action) => {

  switch(action.type) {
    default:
      return state;
  }
}

export default videoRefsReducer;